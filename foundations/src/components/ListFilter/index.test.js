describe('list filter component', () => {
  it('should render properly', () => {
    expect.assertions(1)
    expect(true).toBeFalsy();
  })

  it('should invoke parent callback on click', () => {
    expect.assertions(1)
    expect(true).toBeFalsy();
  })
})
