import { render, screen } from '@testing-library/react';

describe('ListItem component', () =>{
  test('It should render a price', () => {
    expect(false).toBeTruthy();
  });

  test('It should render a date', () => {
    expect(false).toBeTruthy();
  });

  test('It should render a title', () => {
    expect(false).toBeTruthy();
  });
});
